{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "Server=(localdb)\\mssqllocaldb;Database=TimeClockDb;Trusted_Connection=True;"
  },
  // Origins allowed by CORS in development.
  // Must match the port Vite uses (configured in vite.config.js → server.port).
  // Add entries here if you ever need to test from a different local port.
  "AllowedOrigins": [
    "http://localhost:5173"
  ]
}
